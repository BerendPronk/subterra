// Subterra general functions
const subterra = (() => {

  // Initialize subterra client-side script
  const init = () => {
    form.init();
  };

  // Export functions
  return {
    init: init
  }
})();

// Initialize subterra
subterra.init();
